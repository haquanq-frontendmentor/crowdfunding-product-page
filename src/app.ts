import { setupPledgeModal } from "./libs/setupPledgeModal";
import { setupMobileMenu } from "./libs/setupMobileMenu";
import { setupPledgeIntro } from "./libs/setupPledgeIntro";

setupMobileMenu();
setupPledgeIntro();
setupPledgeModal();
